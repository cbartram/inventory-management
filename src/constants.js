/**
 * Set of constants used throughout the Application
 * @type {string}
 */
const DYNAMODB_TABLE_NAME = 'inventory';
const REDIS_HOST = 'localhost';

module.exports = {
  REDIS_HOST,
  DYNAMODB_TABLE_NAME,
};
